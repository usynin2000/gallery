# 🎬 Руководство по работе с Manim в Jupyter Notebook

## 🚀 Быстрый старт

### 1. Установка зависимостей Python
```bash
uv sync
```

### 2. Установка LaTeX для работы с MathTex

**⚠️ Важно:** Если вы планируете использовать `MathTex` для математических формул, необходимо установить LaTeX.

#### Шаг 1: Установка BasicTeX
```bash
# Установка BasicTeX (минимальная версия, ~100 МБ)
brew install --cask basictex
```

#### Шаг 2: Настройка PATH (выполните один раз после установки BasicTeX)
```bash
eval "$(/usr/libexec/path_helper)"
```

#### Шаг 3: Установка необходимых пакетов LaTeX

**🚀 Быстрый способ (рекомендуется):**

Используйте готовый скрипт для автоматической установки всех пакетов:
```bash
./setup_latex.sh
```

**📝 Ручной способ:**

**Найдите путь к tlmgr** (версия может отличаться):
```bash
# Автоматически найти путь к tlmgr
TEXLIVE_PATH=$(find /usr/local/texlive -name tlmgr -type f 2>/dev/null | head -1 | xargs dirname)
echo "Найден путь: $TEXLIVE_PATH"
```

**Или используйте стандартный путь** (для TeX Live 2025):
```bash
TEXLIVE_PATH="/usr/local/texlive/2025basic/bin/universal-darwin"
```

**Установите все необходимые пакеты одной командой:**
```bash
# Обновление менеджера пакетов
sudo $TEXLIVE_PATH/tlmgr update --self

# Установка всех необходимых пакетов для Manim
sudo $TEXLIVE_PATH/tlmgr install \
  standalone \
  preview \
  dvisvgm \
  amsmath \
  amssymb \
  babel-english \
  babel \
  latex-bin \
  xcolor \
  geometry \
  ulem \
  amscls \
  amsfonts

# Обновление базы данных пакетов LaTeX (важно!)
sudo $TEXLIVE_PATH/mktexlsr
```

#### Шаг 4: Проверка установки
```bash
# Проверка доступности LaTeX
which latex
# Должно вывести: /Library/TeX/texbin/latex

# Проверка доступности dvisvgm
which dvisvgm
# Должно вывести путь к dvisvgm

# Проверка установленных пакетов
$TEXLIVE_PATH/tlmgr list --only-installed | grep -E "(standalone|preview|dvisvgm)"
```

### 3. Запуск Jupyter Lab
```bash
uv run jupyter lab
```

**Важно:** После установки LaTeX пакетов:
1. Обновите базу данных пакетов командой `mktexlsr` (см. выше)
2. Перезапустите ядро Jupyter в первый раз, чтобы изменения вступили в силу

Или если хотите использовать классический Jupyter Notebook:
```bash
uv run jupyter notebook
```

### 4. Откройте файл
После запуска Jupyter откройте файл `manim_playground.ipynb`

## ✅ Чеклист для настройки на новом Mac

Если вы настраиваете проект на новом компьютере, выполните следующие шаги:

- [ ] Установлен Python 3.12+ и `uv` (менеджер пакетов)
- [ ] Выполнена команда `uv sync` для установки зависимостей Python
- [ ] Установлен Homebrew (если планируете использовать MathTex)
- [ ] Установлен BasicTeX через `brew install --cask basictex`
- [ ] Выполнена команда `eval "$(/usr/libexec/path_helper)"` для обновления PATH
- [ ] Установлены все необходимые пакеты LaTeX через `./setup_latex.sh` (или вручную через `tlmgr install`)
- [ ] Выполнена команда `mktexlsr` для обновления базы данных пакетов
- [ ] Проверена доступность `latex` и `dvisvgm` через `which`
- [ ] Запущен Jupyter Lab через `uv run jupyter lab`
- [ ] Открыт файл `manim_playground.ipynb`
- [ ] Запущена ячейка инициализации и проверено, что LaTeX найден

## 💡 Как работать

### Быстрый рабочий процесс:

1. **Запустите первые две ячейки** (настройка окружения и функция render_scene)
2. **Экспериментируйте** с примерами или создавайте свои анимации
3. **Просматривайте результат** прямо в браузере - видео встроится в notebook

### Пример использования:

```python
class MyScene(Scene):
    def construct(self):
        circle = Circle()
        self.play(Create(circle))
        self.wait(1)

render_scene(MyScene)  # Автоматически отрисует и покажет
```

## 🎯 Преимущества этого подхода

✅ **Быстрая итерация** - изменили код, запустили ячейку, увидели результат  
✅ **Визуальный фидбек** - видео встраивается прямо в notebook  
✅ **Низкое качество для тестов** - быстрый рендеринг по умолчанию  
✅ **Высокое качество для финала** - `render_scene(MyScene, quality="high_quality")`  
✅ **Документирование** - можете добавлять markdown с заметками  
✅ **Экспериментирование** - легко пробовать разные варианты  

## ⚙️ Настройка качества

```python
# Для быстрых тестов (по умолчанию)
render_scene(MyScene, quality="low_quality")

# Для промежуточных результатов
render_scene(MyScene, quality="medium_quality")

# Для финальных видео
render_scene(MyScene, quality="high_quality")
```

## 📁 Где сохраняются видео

Все видео сохраняются в папке `media/videos/` внутри проекта. 
Путь выводится в notebook после рендеринга.

## 🎨 Полезные ресурсы

- [Официальная документация Manim Community](https://docs.manim.community/)
- [Галерея примеров](https://docs.manim.community/en/stable/examples.html)
- [Справочник API](https://docs.manim.community/en/stable/reference.html)
- [Discord сообщество](https://www.manim.community/discord/)

## 💡 Советы по продуктивности

1. **Начинайте с low_quality** для быстрого прототипирования
2. **Используйте self.wait()** между анимациями для пауз
3. **Комбинируйте анимации** с помощью `self.play(anim1, anim2)` для одновременного выполнения
4. **Группируйте объекты** с помощью `VGroup()` для совместного управления
5. **Сохраняйте примеры** - создавайте новые ячейки для экспериментов

## 🔧 Альтернативный способ (через командную строку)

Если хотите рендерить из Python файлов:
```bash
uv run manim -pql scene.py SquareToCircle
```

Где:
- `-p` - preview (открыть после рендеринга)
- `-q` - quality (l=low, m=medium, h=high, k=4k)
- `-l` - low quality

## 🎓 Следующие шаги

1. Откройте `manim_playground.ipynb`
2. Запустите все примеры
3. Измените их и посмотрите что получится
4. Создайте свою первую анимацию!

Удачи в создании красивых математических анимаций! 🚀

